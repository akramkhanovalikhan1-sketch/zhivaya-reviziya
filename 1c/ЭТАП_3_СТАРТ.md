# Этап 3 — в чате по одному шагу

Не делайте всё сразу. Сейчас **шаг 6**. Шаги 1–5 закрыты.

**База:** только **тестовая** копия «1С:Розница». Основную не открывать.  
**Куда писать:** расширение `ЖиваяРевизия` (префикс `зр`), не снятие с поддержки основной конфигурации.

Критерий всего этапа 3 (позже, на тестовой публикации): `GET http://{сервер}/{тестовая_база}/hs/tsd/ping` → `ok: true`.

## Шаг 1 — база кода + перечисление

### 1.1. Папка проекта

На машине с 1С (если ещё нет клона):

```
git clone https://github.com/akramkhanovalikhan1-sketch/zhivaya-reviziya.git
```

Если папка уже есть:

```
git pull
```

Архив не копировать. В Cursor: File → Open Folder → эта папка.

### 1.2. Что добавить в конфигураторе

Откройте конфигуратор **тестовой** базы «1С:Розница» (копия, не основная).

Если база на поддержке поставщика — объекты кладём в **расширение**, не в основную конфигурацию.

Окно «Новое расширение конфигурации»:

| Поле | Значение |
|---|---|
| Имя | `ЖиваяРевизия` |
| Синоним | Живая ревизия |
| Префикс | `зр` |
| Назначение | Адаптация |

ОК. В списке расширений выделите `ЖиваяРевизия` → **Открыть** (или двойной щелчок). Откроется **второе** дерево — только расширение. В корне Розницы слева ничего не добавлять.

1. В дереве расширения: **Перечисления** → правая кнопка → **Добавить**.
2. Вкладка **Основные** (имя самого объекта, не статусов):
   - **Имя:** `СтатусыЗонРевизии` (если слева само появится `зр` — не стирайте).
   - **Синоним:** Статусы зон ревизии
   - Комментарий оставьте пустым.
3. Вкладка **Данные** — сюда четыре значения. Кнопка добавить. В поле **Имя** без пробелов:

| Имя | Синоним |
|---|---|
| `Свободна` | Свободна |
| `ВРаботе` | В работе |
| `Закрыта` | Закрыта |
| `Перепроверка` | Перепроверка |

Если в дереве уже висит перечисление `Свободна` — удалите его (правая кнопка → Удалить) и создайте заново с именем `СтатусыЗонРевизии`. Статусы не пишут в поля Синоним и Комментарий на вкладке Основные.

Подключать модули и HTTP на этом шаге **не нужно**.

### Как проверить

В дереве: Перечисления → `СтатусыЗонРевизии` → 4 значения. Имена как в таблице, не «В работе» в поле Имя.

Напишите в чат **готово** (или скрин дерева) — шаг 2: регистр `СессииЗонРевизии`.

## Шаг 2 — регистр `СессииЗонРевизии`

Окна: **слева** `ЖиваяРевизия` — сюда все новые объекты. **Справа** «Конфигурация базы данных» не редактировать (это снимок, не расширение).

Чтобы взять `ФизическиеЛица`: закройте правое окно. Меню **Конфигурация → Открыть конфигурацию** (основная Розница). Справочники → `ФизическиеЛица` → правая кнопка → **Добавить в расширение**. Выбор расширения: `ЖиваяРевизия`.

1. В дереве **слева** (`ЖиваяРевизия`): **Регистры сведений** → правая кнопка → **Добавить**.
3. Вкладка **Основные**:
   - Имя: `СессииЗонРевизии`
   - Синоним: Сессии зон ревизии
   - Периодичность: **Непериодический**
   - Режим записи: **Независимый**
4. Вкладка **Данные**:

**Измерения** (одна строка):

| Имя | Тип |
|---|---|
| `НомерЗоны` | Строка, длина 20 |

**Ресурсы** (семь строк):

| Имя | Тип |
|---|---|
| `НомерСессии` | Число, длина 5, точность 0 |
| `Сотрудник` | СправочникСсылка.ФизическиеЛица |
| `ВремяСтарта` | Дата (дата и время) |
| `ВремяФиниша` | Дата (дата и время) |
| `Статус` | ПеречислениеСсылка.СтатусыЗонРевизии |
| `ЗаблокированныйСотрудник` | СправочникСсылка.ФизическиеЛица |
| `Карантин` | Булево |

Реквизитов у этого регистра нет. Ctrl+S.

Шаг 2 закрыт 2026-09-21: измерение `НомерЗоны`, ресурсы `НомерСессии`, `Сотрудник`, `ВремяСтарта`, `ВремяФиниша`, `Статус`, `ЗаблокированныйСотрудник`, `Карантин`.

## Шаг 3 — регистр `ИсторияСессийЗонРевизии`

Тот же левый дерево `ЖиваяРевизия`. Другие регистры пока не создавайте.

1. **Регистры сведений** → правая кнопка → **Добавить**.
2. Вкладка **Основные**:
   - Имя: `ИсторияСессийЗонРевизии`
   - Синоним: История сессий зон ревизии
   - Периодичность: **Непериодический**
   - Режим записи: **Независимый**
3. Вкладка **Данные**:

**Измерения** (две строки):

| Имя | Тип |
|---|---|
| `НомерЗоны` | Строка, длина 20 |
| `НомерСессии` | Число, длина 5, точность 0 |

**Ресурсы** (четыре строки):

| Имя | Тип |
|---|---|
| `Сотрудник` | СправочникСсылка.ФизическиеЛица |
| `ВремяСтарта` | Дата и время |
| `ВремяФиниша` | Дата и время |
| `Статус` | ПеречислениеСсылка.СтатусыЗонРевизии |

Реквизитов нет. Имя — в поле **Имя**, тип — только в поле **Тип**. Ctrl+S.

Шаг 3 закрыт 2026-09-21: измерения `НомерЗоны` + `НомерСессии`, ресурсы `Сотрудник`, `ВремяСтарта`, `ВремяФиниша`, `Статус`.

## Шаг 4 — регистр `ИсторияСканированияТСД`

Сначала справочник в расширение (для типа товара):

1. Меню **Конфигурация → Открыть конфигурацию** (основная Розница).
2. Справочники → `Номенклатура` → правая кнопка → **Добавить в расширение** → `ЖиваяРевизия`.
3. Все новые поля снова только в левом дереве `ЖиваяРевизия`.

Затем **Регистры сведений** → Добавить:

- Имя: `ИсторияСканированияТСД`
- Периодичность: **Непериодический**
- Режим записи: **Независимый**

**Измерения** (семь):

| Имя | Тип |
|---|---|
| `МоментВремени` | Дата и время |
| `ИдЗаписи` | УникальныйИдентификатор |
| `ИдентификаторТСД` | Строка 20 |
| `Сотрудник` | СправочникСсылка.ФизическиеЛица |
| `НомерЗоны` | Строка 20 |
| `НомерСессии` | Число 5, 0 |
| `Номенклатура` | СправочникСсылка.Номенклатура |

**Ресурс** (один):

| Имя | Тип |
|---|---|
| `Количество` | Число, длина 15, точность 3 |

**Реквизит** (один — не в ресурсы):

| Имя | Тип |
|---|---|
| `СчитанныйШтрихкод` | Строка 48 |

Тип сразу с **Строки**. Ctrl+S.

Шаг 4 закрыт 2026-09-22: семь измерений, ресурс `Количество` (15,3), реквизит `СчитанныйШтрихкод` (строка 48).

## Шаг 5 — регистр `ЗаморозкаНоменклатурыРевизии`

Только дерево `ЖиваяРевизия`. `Номенклатура` уже в расширении.

1. **Регистры сведений** → Добавить.
2. Имя: `ЗаморозкаНоменклатурыРевизии`
3. Периодичность: **Непериодический**. Режим записи: **Независимый**.

**Измерение:**

| Имя | Тип |
|---|---|
| `Номенклатура` | СправочникСсылка.Номенклатура |

**Ресурс:**

| Имя | Тип |
|---|---|
| `ДействуетДо` | Дата и время |

Реквизитов нет. Ctrl+S.

Шаг 5 закрыт 2026-09-22: измерение `Номенклатура`, ресурс `ДействуетДо`.

## Шаг 6 — регистр `ГруппыАналоговНоменклатуры`

Только дерево `ЖиваяРевизия`.

1. **Регистры сведений** → Добавить.
2. Имя: `ГруппыАналоговНоменклатуры` (чтобы слева не остался `зр_РегистрСведений1` — Tab после имени).
3. Непериодический, независимый.

**Измерение:**

| Имя | Тип |
|---|---|
| `Номенклатура` | СправочникСсылка.Номенклатура |

**Ресурс:**

| Имя | Тип |
|---|---|
| `ОсновнаяНоменклатура` | СправочникСсылка.Номенклатура |

Реквизитов нет. Ctrl+S. Скрин имени в дереве и вкладки **Данные**.
